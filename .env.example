# ============================================================================
# Supabase設定
# ============================================================================
# SupabaseプロジェクトのURL（Dashboard > Settings > API > Project URL）
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Supabase匿名キー（Dashboard > Settings > API > Project API keys > anon/public）
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabaseサービスロールキー（Dashboard > Settings > API > Project API keys > service_role）
# ⚠️ 警告: このキーは絶対に公開しないでください。サーバーサイドでのみ使用。
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# 本番環境での追加設定（必須）
# ============================================================================
# サイトのベースURL（BASE OAuthリダイレクト、メール通知等で使用）
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Vercel Cron認証用シークレット（ランダムな文字列を生成）
# 生成例: openssl rand -base64 32
CRON_SECRET=your-random-secret-token

# Supabase Auth JWT シークレット（Dashboard > Settings > API > JWT Settings > JWT Secret）
SUPABASE_AUTH_SECRET=your-jwt-secret

# ============================================================================
# AI機能（オプション）
# ============================================================================
# Claude AI APIキー（https://console.anthropic.com/）
ANTHROPIC_API_KEY=your-anthropic-api-key
